import React from "react";
import Form from "../components/component/Form";

type Props = {};

const ProfilePage = (props: Props) => {
  return <Form mode="update" />;
};

export default ProfilePage;
